# Harness profile: Claude Code. See specs/02-harness-profiles.md.
# Field paths verified against a real Claude Code 2.1.201 capture (M0), except
# PermissionRequest/PermissionDenied (bypassed in headless — still provisional).

[harness]
name      = "claude-code"
adapter   = "command-hook"
transport = "stdin-json"          # stdin-json | argv | env | file

[events]
SessionStart       = "session_start"
UserPromptSubmit   = "turn_start"
PreToolUse         = "tool_pre"
PostToolUse        = "tool_post"
PostToolUseFailure = "tool_error"
PermissionRequest  = "permission_request"
PermissionDenied   = "permission_denied"
Stop               = "turn_end"
SubagentStop       = "subagent_stop"       # Claude Code has no SubagentStart; stop annotates
PreCompact         = "compaction"
SessionEnd         = "session_end"

[fields]
session_id         = "session_id"
tool_name          = "tool_name"
tool_input         = "tool_input"
tool_output        = "tool_response"
tool_use_id        = "tool_use_id"
transcript         = "transcript_path"
permission_mode    = "permission_mode"
cwd                = "cwd"
agent_id           = "agent_id"            # set on SubagentStop + subagent-interior tools
agent_type         = "agent_type"          # SubagentStop, e.g. "Explore"
compaction_trigger = "trigger"             # PreCompact (provisional — not yet captured)

[registration]
kind    = "claude-code-settings"
command = "weave-agent-adapter hook --harness claude-code"  # installer appends --event <event>
events  = [
  "SessionStart", "UserPromptSubmit", "PreToolUse", "PostToolUse",
  "PostToolUseFailure", "PermissionRequest", "PermissionDenied", "Stop",
  "SubagentStop", "PreCompact", "SessionEnd",
]
